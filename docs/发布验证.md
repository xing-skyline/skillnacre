# 1.1.0 发布验证记录

验证日期：2026-09-15。环境：Windows x64，系统构建 26200，Node.js 22.13.1。测试使用独立示例目录。

## 检查结果

| 检查 | 实际结果 |
| --- | --- |
| `npm test` | 40 项通过，0 失败 |
| 打包程序运行 `tests/desktop-smoke.mjs` | 23 个桌面检查点通过，页面错误 0 |
| `tests/portable-smoke.mjs` | 便携版自动解压、中文窗口、独立主源读取、真实目录软链接创建，共 4 项通过 |
| `tests/github-smoke.mjs` | 浅克隆、子目录导入、重启后来源保留、提交记录、上游复查，共 5 项通过 |
| `tests/capture-demo.mjs` | 使用 6 个虚构技能生成 5 张截图；备份目录留空时浏览和实际链接正常 |
| `npm audit` | 检查时报告的已知依赖漏洞数量为 0 |
| 运行时代码核对 | `app.asar` 中 7 个核心模块和 2 个 Electron 文件与发布源代码逐字节一致 |
| 包内容与许可 | 核对包含范围，未打入测试数据或个人技能；软件许可证及 108 个生产依赖的声明已随包提供 |
| Windows 签名状态 | 安装版和便携版均为 `NotSigned` |

核心回归覆盖链接修复、跨盘备份、预览后内容变化、目录重定向、恢复检查、同步冲突、中断整理、来源基线和自动路径识别等。发布准备新增了两个默认设置检查：新安装不带个人备份路径，已有来源、备份与工具路径继续保留。

桌面检查覆盖浏览、搜索、编辑、真实链接、主源切换、组合、矩阵、恢复差异、恢复执行、共享路径发现、来源更新、冲突处理、自动识别应用和 1080 × 740 窗口布局。

GitHub 集成验证使用公开仓库 `obra/superpowers` 的 `skills/systematic-debugging` 子目录，检查时实际提交为 `b36e0829c6d0140e93cfef2ca599b1b07d4a7797`。该测试下载的技能不随软件分发。

## 发行文件

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `SkillNacre-Setup-1.1.0.exe` | 114,072,236 | `968fbb12adaf2cd2b2e3d3803c5c7b9e8c80a4058923760afd9b74f518111376` |
| `SkillNacre-Portable-1.1.0.exe` | 113,850,587 | `97b91ad53646d36ab5fc10b21c869d872d30801c180f05ab84f114c3ed6ad88d` |

Releases 同时提供 `SHA256SUMS.txt`。校验值对应本次发布构建；重新打包产生的文件不保证逐字节相同。

## 公开内容检查

公开仓库使用生成的示例截图。真实环境的旧截图、含个人路径的验证记录、内部调研笔记、本机配置、下载快照和测试临时数据已排除。安装包仅含应用运行文件、生产依赖、图标和许可文件。

## 未覆盖的范围

没有在每一个 AI 工具的真实模型会话中逐一验证加载效果。上述链接检查证明文件系统指向正确，工具能否加载仍取决于版本与配置。

本次未完成干净虚拟机上的安装 / 卸载向导测试、发布者代码签名、Windows ARM64、macOS、Linux、WSL 或远程文件系统的完整验证。依赖审计结果属于当次检查，不是完整安全审计或未来无漏洞的保证。
